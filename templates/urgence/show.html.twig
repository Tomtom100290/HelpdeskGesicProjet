{% extends 'base.html.twig' %}

{% block title %}Urgence{% endblock %}

{% block body %}
    <h1>Urgence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ urgence.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ urgence.niveau }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ urgence.libelle }}</td>
            </tr>
            <tr>
                <th>Prompt</th>
                <td>{{ urgence.prompt }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ urgence.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_urgence_index') }}">back to list</a>

    <a href="{{ path('app_urgence_edit', {'id': urgence.id}) }}">edit</a>

    {{ include('urgence/_delete_form.html.twig') }}
{% endblock %}
