<form method="post" action="{{ path('app_logiciel_client_delete', {'id': logiciel_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logiciel_client.id) }}">
    <button class="btn">Delete</button>
</form>
